{
  "name": "time-delay",
  "module": "server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "build": "bun build main.ts --outdir . --target browser --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
